import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.coem.ionicazureb2c',  
  appName: 'ionic-azure-b2c', 
  webDir: 'www',  
  server: {
    androidScheme: 'https',  
  },
};

export default config;
